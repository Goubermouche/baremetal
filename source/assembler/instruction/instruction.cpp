#include "instruction.h"

namespace baremetal {
	//instruction::instruction(enum opcode opcode)                                  : opcode(opcode), operands{} {}
	//instruction::instruction(enum opcode opcode, operand a)                       : opcode(opcode), operands{ a } {}
	//instruction::instruction(enum opcode opcode, operand a, operand b)            : opcode(opcode), operands{ a, b } {}
	//instruction::instruction(enum opcode opcode, operand a, operand b, operand c) : opcode(opcode), operands{ a, b, c } {}
} // namespace baremetal
